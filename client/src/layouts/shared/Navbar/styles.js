import styled from 'styled-components';
import { Columns } from 'react-bulma-components';

export const FullWidthColumn = styled(Columns)`
  width: 100%;
  padding: 20px 0px 10px 10px;
`;

export const Logo = styled.img``;
